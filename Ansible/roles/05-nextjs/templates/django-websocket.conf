# /etc/nginx/conf.d/django.conf

upstream app_servers {
    server 127.0.0.1:8000;
    # server 127.0.0.1:8001;
}

upstream ws_servers {
    server 127.0.0.1:8000;
    # server 127.0.0.1:8001;
}

server {
    server_name {{domain_name}};
    listen  80;

    # Redirect to no www.
    set $https_redirect 0;
    if ($server_port = 80) { set $https_redirect 1; }
    if ($host ~ '^www\.') { set $https_redirect 1; }
    if ($https_redirect = 1) {
        return 301 https://{{domain_name}}$request_uri;
    }

    location / {
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;

        # Enable this if and only if you use HTTPS
        proxy_set_header X-Forwarded-Proto https;

        # pass the Host: header from the client right along so redirects
        # can be set properly within the Rack application
        proxy_set_header Host $host;

        # we don't want nginx trying to do something clever with
        # redirects, we set the Host: header above already.
        proxy_redirect off;
        proxy_pass http://app_servers;
    }

    location /ws/ {
        proxy_set_header   X-Forwarded-For $remote_addr;
        proxy_set_header   Host $http_host;
        proxy_http_version 1.1;
        proxy_set_header   Upgrade $http_upgrade;
        proxy_set_header   Connection "upgrade";
        proxy_pass         "http://ws_servers";
    }

    location /static {
        autoindex off;
        alias /usr/share/nginx/html/static/;
    }

    location /media {
        autoindex off;
        alias /usr/share/nginx/html/media/;
    }

    location /.well-known {
        alias /usr/share/nginx/html/.well-known/;
    }
    
    listen 443 ssl; # managed by Certbot
    ssl_certificate /etc/letsencrypt/live/flutter.ottawastem.com/fullchain.pem; # managed by Certbot
    ssl_certificate_key /etc/letsencrypt/live/flutter.ottawastem.com/privkey.pem; # managed by Certbot
    include /etc/letsencrypt/options-ssl-nginx.conf; # managed by Certbot
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem; # managed by Certbot
}