# Install Docker Engine (https://docs.docker.com/engine/install/ubuntu/)
- name: Install prerequisite packages
  ansible.builtin.apt:
    pkg:
      - ca-certificates
      - curl
    state: present
    update_cache: true

- name: Create keyrings directory
  ansible.builtin.file:
    path: /etc/apt/keyrings
    state: directory
    mode: '0755'

- name: Add Docker APT signing key
  ansible.builtin.get_url:
    url: https://download.docker.com/linux/ubuntu/gpg
    dest: /etc/apt/keyrings/docker.asc
    mode: '0644'
    force: true
    
# - name: Add Docker deb to /etc/apt/sources.list repository
#   ansible.builtin.apt_repository:
#     repo: 'deb [arch={{ "arm64" if ansible_architecture == "aarch64" else "amd64" }} signed-by=/etc/apt/keyrings/docker.asc] https://download.docker.com/linux/ubuntu {{ ubuntu_code_name }} stable'
#     filename: docker
#     state: present

# - name: Install Docker Engine packages
#   apt:
#     pkg:
#       - docker-ce
#       - docker-ce-cli
#       - containerd.io
#       - docker-buildx-plugin
#       - docker-compose-plugin
#     state: present
#     update_cache: yes

# - name: Add {{ ansible_user }} to the docker group
#   user:
#     name: '{{ ansible_user }}'
#     groups: docker
#     append: yes

# - name: Start Docker
#   service:
#     name: docker
#     enabled: yes
#     state: started

# # Clean up
# - name: Remove useless packages from the cache
#   apt:
#     autoclean: yes

# - name: Remove dependencies that are no longer required
#   apt:
#     autoremove: yes
